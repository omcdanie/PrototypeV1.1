sudo: required

language: node_js
node_js:
  - "8"

services:
  - docker

before_install:
  - docker-compose --version
  - ./bucketful-build.sh

deploy:
  - provider: script
    script: bash deploy.sh
